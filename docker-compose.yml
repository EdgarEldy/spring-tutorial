services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      JDBC_URL: jdbc:postgresql://postgres_main:5432/spring_tutorial
      JDBC_USERNAME: admin
      JDBC_PASSWORD: admin
    networks:
      - pg_net

networks:
  pg_net:
    external: true
